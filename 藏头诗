本题要求编写一个解密藏头诗的程序。
#include <stdio.h>

int main()
{
    char str[100][100], arr[10];
    int i = 0;

    for( i = 0; i < 4; i++ ) {
        scanf("\n");
        scanf("%s", &str[i]);
        arr[2*i] = str[i][0];
        arr[2*i+1] = str[i][1];
    }
    arr[8] = '\0';
    printf("%s", arr);

    return 0;
}
